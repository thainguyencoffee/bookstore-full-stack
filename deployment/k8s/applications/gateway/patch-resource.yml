apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore-gateway
spec:
  template:
    spec:
      containers:
        - name: bookstore-gateway
          resources:
            requests:
              memory: 756Mi
              cpu: "0.1"
            limits:
              memory: 756Mi
              cpu: "2"