apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bookstore-gateway-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bookstore-gateway
                port:
                  number: 80