apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bookstore-edge-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bookstore-edge
                port:
                  number: 80